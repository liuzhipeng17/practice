客户端：

# log(采用Logging)
# login(鉴权，base64, hashlib)
# 发送命令：1）查看当前目录下文件(ls -l)列出文件的大小等
            2) 切换目录
            3) 上传下载文件（保证文件一致性hashlib)

# 客户端界面显示：1）进度条
                  2）usage


服务端：

# 用户鉴权
# 支持多用户并发登录
# 接收命令，解析命令，处理命令结果(subprocess)
# 返回结果给客户端