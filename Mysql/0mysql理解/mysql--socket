# mysql就是一个文件管理系统软件（包含客户端，服务端）
    # 服务端软件：
        # socket服务
        # 本地文件操作
        # 解析指令--换成代码（指令就是客户端和服务端交流语言，sql语句）
    # 客户端软件
        # socket客户端
        # 发送指令
        # 解析指令（有自己的语法规则）

        DBMS:文件管理系统

# 简单来说： mysql就是：我发送指令，mysql帮我进行远程文件操作，不用我进行操作


# 掌握： mysql安装，连接， 操作