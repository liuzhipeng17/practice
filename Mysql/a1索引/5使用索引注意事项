1 避免使用select *  ---基础
2 count(1)或者count(主键) 代替count(*) ---基础
3 创建表尽量使用char, 代替varchar; 使用varchar,尽量放在表列后面 ---基础
4 组合索引代替多个单列索引（前提是：经常使用改组合查询）
5 尽量使用短索引
    create index ix_xx on table1(title(16))
    -- title后面的字符是一样的，前16个字符不一样
6 使用join代替子查询
7 连表时注意类型要一致
8 不重复的列，创建索引是最好的；重复少的列不适合建索引 ---基础